{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "risk.case_opened.v1.json",
  "title": "Alpha One \u2014 event risk.case_opened v1",
  "description": "V1 catalog event `risk.case_opened` (v1). Envelope per EVT-03 (see envelope.schema.json). Producer/consumers per contracts/events/catalog.md: consumers = ADM (queue), AUD, ANA (risk_summary daily counts). Payload field values are INFERRED from consumer requirements; the sheet fixes only the envelope (EVT-03) \u2014 field confirmation is an open owner decision (catalog.md).",
  "allOf": [
    {
      "$ref": "envelope.schema.json#/$defs/envelope"
    },
    {
      "properties": {
        "type": {
          "const": "risk.case_opened"
        },
        "version": {
          "const": 1
        },
        "payload": {
          "type": "object",
          "properties": {
            "case_id": {
              "$ref": "envelope.schema.json#/$defs/ULID"
            },
            "tenant_id": {
              "$ref": "envelope.schema.json#/$defs/ULID"
            },
            "trader_id": {
              "$ref": "envelope.schema.json#/$defs/ULID"
            },
            "account_ids": {
              "type": "array",
              "items": {
                "$ref": "envelope.schema.json#/$defs/ULID"
              }
            },
            "kind": {
              "type": "string",
              "enum": [
                "manual",
                "breach"
              ]
            },
            "severity": {
              "type": "string",
              "enum": [
                "info",
                "low",
                "medium",
                "high",
                "critical"
              ]
            },
            "payout_hold": {
              "type": "boolean"
            },
            "opened_by": {
              "$ref": "envelope.schema.json#/$defs/ULID"
            },
            "opened_at": {
              "$ref": "envelope.schema.json#/$defs/TimestampMs"
            }
          },
          "additionalProperties": false,
          "required": [
            "case_id",
            "tenant_id",
            "trader_id",
            "account_ids",
            "kind",
            "severity",
            "payout_hold",
            "opened_at"
          ]
        }
      },
      "required": [
        "type",
        "version",
        "payload"
      ]
    }
  ]
}
